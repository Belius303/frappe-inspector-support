name: Publish Benchmark Release

on:
  push:
    tags:
      - "v*-evidence"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Frappe Inspector 1.4.0
          body_path: docs/release-v1.4.0.md
          files: release-v1.4.0/*
